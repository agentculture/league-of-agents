"""The continuous arena lane — decimal positions, role-given speed, time as the
resolver — landing beside the grid engine, not over it (spec cycle 7)."""

from league.engine.continuous.timeline import ScheduledAction, Timeline

__all__ = ["ScheduledAction", "Timeline"]
